= form_for @mypage do |f|
  - if @mypage.errors.any?
    #error_explanation
      %h2= "#{pluralize(@mypage.errors.count, "error")} prohibited this mypage from being saved:"
      %ul
        - @mypage.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
